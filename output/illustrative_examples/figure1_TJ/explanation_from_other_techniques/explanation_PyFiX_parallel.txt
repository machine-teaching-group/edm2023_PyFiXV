 When calling `print` on multiple values, we need to separate the values by commas. Here, in the print statement at the second line, `"Xx x"` and `a` are not separated by a comma. To fix the error, add a comma in between them.